; Pin the standard npm registry for this project. A user-level npmrc pointing
; at a third-party mirror (e.g. mirrors.tencentyun.com) must never leak mirror
; URLs into package-lock.json — CI installs with `npm ci` from these resolved
; URLs and fails when the mirror is unreachable.
registry=https://registry.npmjs.org/
